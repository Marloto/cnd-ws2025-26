server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/Marloto/spring-boot-config-example.git"
          default-label: main
