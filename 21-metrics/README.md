# Metrics

Download latest components.yaml from https://github.com/kubernetes-sigs/metrics-server/releases

Add `--kubelet-insecure-tls` in `metrics-server` Deployment

Apply with `kubectl apply -f components.yaml`
